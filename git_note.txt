1. git status 
2. git add .
git commit -m "Added index.html" -m "some desciption"
git push

# branching
1 git branch (show available branches)
2 git checkout (to switch between branches)
3.git checkout -b feature_readme  (creates and switches to new branch)
4.git push
5.git branch -d feature (delete branch)